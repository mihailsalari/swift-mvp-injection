import UIKit

protocol {{ module_info.name }}PresenterProtocol {
    func present()
}

class {{ module_info.name }}Presenter: {{ module_info.name }}PresenterProtocol {
    private weak var view: (FaceDetectionViewControllerProtocol & UIViewController)!

    init(view: {{ module_info.name }}ViewControllerProtocol & UIViewController) {
        self.view = view
    }

    func present() {
        // Fetch data or smth else...
    }
}
